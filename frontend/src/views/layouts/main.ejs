<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/global.css">
    <title><%= title %></title>
</head>
<body>
    <% if (currentPage !== 'home' && currentPage !== 'sign-in') { %>
        <%- include('layouts/navbar') %>
    <% } %>
    <%- body %>
</body>
</html>
